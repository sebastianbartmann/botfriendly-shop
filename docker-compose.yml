services:
  ecom-llm-checker:
    container_name: ecom-llm-checker
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8084:8084"
    volumes:
      - ecom_checker_data:/app/data
    restart: unless-stopped

volumes:
  ecom_checker_data:
